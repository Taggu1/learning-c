

#include <stdio.h>

#define N 10
#define NUM_ROWS 10
#define NUM_COLS 10

int main(void) {
    int n,m;
    int a[m][n], (*p)[n];

    for (p = a; p < a + m; p++)
    {
        (*p)[1] == 0;
    }
    

}